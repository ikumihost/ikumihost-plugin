{
  "name": "ai-host-mcp",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --format=esm --banner:js=\"import{createRequire}from'module';const require=createRequire(import.meta.url);\" --outfile=dist/index.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "mime-types": "^2.1.35",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
